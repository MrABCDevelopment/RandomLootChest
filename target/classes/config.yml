# Enable or disable update message
update-checker: true

# Opens random chest
random-chests: false

# How many rows will have all chests
random-chest-rows: 3

# Combat Section
# Enable - if true, it is enabled, otherwise it is disabled
# Time - in seconds, when they cannot open any chest
combat:
  enable: false
  time: 20

chest-add-sound: ENTITY_EXPERIENCE_ORB_PICKUP

# In database, this plugin saves information about cooldowns to chests. If it is disabled, all cooldowns after reload plugin
# or server shutdown, will disappear.
database:
  use: false
  type: "YAML"
  #in seconds
  auto-save-time: 300
  host: "localhost"
  user: "user"
  password: "password"
  database: "database"
  port: 3306

wand-item:
  displayName: '&6Chest Wand'
  permission-to-use: true
